[pytest]
minversion = 7.0
asyncio_mode = auto
testpaths =
    app
    scripts
    tests
addopts =
    --cov=app
    --cov=scripts
    --cov-report=term-missing
    --cov-report=xml:coverage.xml
    --cov-report=html:coverage_html
