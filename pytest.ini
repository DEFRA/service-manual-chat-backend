[pytest]
minversion = 7.0
asyncio_mode = auto
testpaths =
    app
    tests
addopts =
    --cov=app
    --cov-report=term-missing
    --cov-report=xml:coverage.xml
    --cov-report=html:coverage_html
